# Program: Hands-on Map (Easy)

# Lists
list1 = [1, 2, 3]
list2 = [4, 5, 6]

# Add numbers of two lists
add_list = list(map(lambda x, y: x + y, list1, list2))
print("Addition list:", add_list)

# Square of numbers in another list
nums = [2, 3, 4]
square_list = list(map(lambda x: x ** 2, nums))
print("Square List:", square_list)